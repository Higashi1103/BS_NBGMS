<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">

<head>
<title>技術者管理システム</title>
<meta charset="utf-8">

<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script
	src="https://cdn.jsdelivr.net/npm/bootstrap@3.4.1/dist/js/bootstrap.min.js"></script>
<script
	src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
<script
	src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap.min.js"></script>
<link
	href="https://cdn.jsdelivr.net/npm/bootstrap@3.4.1/dist/css/bootstrap.min.css"
	rel="stylesheet">
<link
	href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap.min.css"
	rel="stylesheet">

<style>
.navbar {
	position: fixed;
	top: 0;
	left: 0;
	width: 100%;
	height: 51px;
	background-color: #f9f9f9;
	color: #000000;
	display: flex;
	align-items: center;
	padding: 0 10px;
	box-sizing: border-box;
	z-index: 1000;
	border-bottom: 1px solid #e5e5e5;
}

a {
	letter-spacing: 0.05em;
}

#title {
	position: absolute;
	left: 10px;
	top: 50%;
	transform: translateY(-50%);
	font-size: 18px;
	color: #777;
}

#user {
	position: absolute;
	right: 20px;
	top: 50%;
	transform: translateY(-50%);
	cursor: pointer;
}

.center {
	text-align: center;
}

.flex {
	display: flex;
}

.overlay {
  display: none;
  position: fixed;        /* 固定在视口，而不是文档流 */
  top: 0;
  left: 0;
  width: 100vw;           /* 覆盖整个可视宽度 */
  height: 100vh;          /* 覆盖整个可视高度 */
  background: rgba(0,0,0,0.45);  /* 半透明遮罩 */
  place-items: center;
  z-index: 9999;          /* 超高层级，保证在所有元素上方 */
}

.dialog {
  background: white;
  border-radius: 12px;
  width: 400px;
  height: auto;
  max-height: 90vh;
  overflow-y: auto;
  padding-bottom: 50px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.3);
  z-index: 10000; /* 比遮罩略高，但一般只要父层够高就行 */
}

.dlgTitle {
  background-color:#d9534f; 
  margin-top:0; 
  margin-bottom:42px; 
  border-radius:12px 12px 0px 0px; 
  padding: 10px; text-align:center; 
  border-bottom:1px solid gray; 
  color:white
}

.dlgTitleMsg {
  background-color:#5bc0de; 
  margin-top:0; 
  margin-bottom:42px; 
  border-radius:12px 12px 0px 0px; 
  padding: 10px; text-align:center; 
  border-bottom:1px solid gray; 
  color:white
}

.update {
	margin-right: 15px;
}

.margin-left {
	margin-left: auto;
}

.margin-bottom {
	display:inline-block;
	margin-bottom: 6px;
}

.float-right {
	float: right;
}

.dropdown-menu {
	display: none;
	position: absolute;
	top: 40px;
	right: 0;
	transform: translateX(-100px);
	background-color: #fff;
	border: 1px solid #ddd;
	border-radius: 5px;
	box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
	min-width: 100px;
	z-index: 2000;
}

.dropdown-menu a {
	display: block;
	padding: 8px 10px;
	color: #333;
	text-decoration: none;
	font-size: 14px;
}

.dropdown-menu a:hover {
	background-color: #f2f2f2;
}

.has-error {
    border-color: #a94442;
    box-shadow: inset 0 1px 1px rgba(0, 0, 0, .075), 0 0 6px rgba(169, 68, 66, .6); ;
}

.has-success {
    border-color: #3c763d;
    box-shadow: inset 0 1px 1px rgba(0, 0, 0, .075), 0 0px 6px rgba(0, 0, 0, .075);
}

.sidebar {
	position: fixed;
	top: 51px;
	left: 0;
	width: 250px;
	height: calc(100% - 51px);
	background-color: #f9f9f9;
	color: rgb(48, 48, 48);
	box-sizing: border-box;
	border-right: 1px solid #e5e5e5;
}

.sidebar-nav div {
	margin: 0;
	padding: 10px;
	border-bottom: 1px solid #e5e5e5;
}

.sidebar-nav div a {
	color: #454545;
	font-size: 14px;
	text-decoration: none;
	display: flex;
	align-items: center;
}

.sidebar-nav div:hover {
	background-color: #e6e6e6;
	color: #fff;
}

.main-content {
	margin-top: 30px;
	margin-left: 240px;
	padding: 40px;
	min-height: calc(100vh - 51px);
	background-color: #fff;
	display: flex;
	justify-content: center;
}

.required-label {
	background-color: #d9534f;
	color: #fff;
	font-size: 12px;
	padding: 1px 5px;
	border-radius: 3px;
	margin-left: 5px;
}

#shousaitable {
	width: 1060px;
	table-layout: auto;
}
</style>
</head>

<body>
	<!-- ヘッド -->
	<nav class="navbar" style="margin-bottom: 0">
		<div id="title">技術者管理システム</div>

		<!-- ログアウトバタン -->
		<div id="user">
			<img src="/image/user.png" height="25px">
			<div class="dropdown-menu" id="dropdownMenu">
				<a href="#">ログアウト</a>
			</div>
		</div>

		<!-- 左ナビ -->
		<div class="sidebar">
			<div class="sidebar-nav">
				<div>
					<a th:href="@{/kurasu}"><img src="/image/kurasu.png"
						height="18px" style="margin-right: 5px;">クラス情報</a>
				</div>
				<div>
					<a th:href="@{/gakusei}"><img src="/image/gakusei.png"
						height="18px" style="margin-right: 5px;">学生情報</a>
				</div>
				<div>
					<a th:href="@{/tensu}"><img src="/image/pointo.png"
						height="18px" style="margin-right: 5px;">ポイント履歴</a>
				</div>
				<div>
					<a th:href="@{/hiaringu}"><img src="/image/hiaringu.png"
						height="18px" style="margin-right: 5px;">ヒアリング履歴</a>
				</div>
				<div>
					<a th:href="@{/shuturyoku}"><img src="/image/shutsuryoku.png"
						height="18px" style="margin-right: 5px;">出力</a>
				</div>
			</div>
		</div>
	</nav>

	<div class="main-content">
		<div id="table-content">
			<div class="col-lg-12">
				<div>
					<div id="overlay" class="overlay" role="dialog" aria-modal="true">
						<div id="dialog" class="dialog" tabindex="-1">
							<h4 class="dlgTitle"></h4>
							<div class="center">
							</div>
							<div id="dialogBtns">
								<form>
								</form>
							</div>
						</div>
					</div>
					<div th:if="${singleKurasu != null}" class="panel panel-default" style="width: 1090px;">
						<div class="panel-heading">クラス更新入力</div>
						<div id="panel-body1" class="panel-body">
							<form id="updateForm" action="/updateKurasuInfo" method="post">
								<input id="kurasuId" name="kurasuId" th:value="${singleKurasu.kurasuId}" type="hidden">
								<label for="name">クラス名前：</label><span class="required-label margin-bottom">必須</span>
								<input id="name" type="text" name="kurasuNamae" th:value="${singleKurasu.kurasuNamae}" class="form-control"></span> <span id="nameError"></span> 
								<br> <br> 
								<label for="contents">コンテンツ：</label><span class="required-label margin-bottom">必須</span>
								<input id="contents" type="text" name="kurasuKamoku" th:value="${singleKurasu.kurasuKamoku}" class="form-control"></span> <span id="contentsError"></span> 
								<br> <br> 
								<label for="instructor">担当先生：</label><span class="required-label margin-bottom">必須</span>
								<input id="instructor" type="text" name="tantoSensei" th:value="${singleKurasu.tantoSensei}" class="form-control"></span> <span id="instructorError"></span> 
								<br> <br> 
								<label for="memo">備考：</label>
								<textarea id="memo" name="memo" th:text="${singleKurasu.memo}" class="form-control"></textarea>
								<br> <br>
								<div class="flex">
									<input type="submit" value="更新" class="btn btn-info margin-left update">
									<input type="button" value="戻る" th:onclick="|location.href='@{/kurasushousai(kurasuId=${singleKurasu.kurasuId})}'|" class="btn btn-warning">
								</div>
							</form>
						</div>
					</div>
					<div th:unless="${singleKurasu != null}" class="panel panel-default" style="width: 1090px;">
						<div class="panel-heading">クラス新規登録</div>
						<div id="panel-body2" class="panel-body">
							<form id="insertForm" action="/insertKurasuInfo" method="post">
								<label for="name">クラス名前:</label><span class="required-label margin-bottom">必須</span>
								<input id="name" type="text" name="kurasuNamae" class="form-control"></span> <span id="nameError"></span> 
								<br> <br>
								<label for="contents">コンテンツ:</label><span class="required-label margin-bottom">必須</span>
								<input id="contents" type="text" name="kurasuKamoku" class="form-control"></span> <span id="contentsError"></span> 
								<br> <br> 
								<label for="instructor">担当先生:</label><span class="required-label margin-bottom">必須</span>
								<input id="instructor" type="text" name="tantoSensei" class="form-control"></span> <span id="instructorError"></span> 
								<br> <br> 
								<label for="memo">備考:</label>
								<textarea id="memo" name="memo" class="form-control"></textarea>
								<br> <br>
								<div class="flex">
									<input type="submit" value="登録" class="btn btn-success margin-left update">
									<input type="button" value="戻る" onclick="location.href='/kurasu'" class="btn btn-warning">
								</div> 
							</form>		
						</div>	
					</div>
					<!-- /.panel-body -->
				</div>
				<!-- /.panel -->
			</div>
		</div>
	</div>
	<script>
		$(function() {
			$('#overlay').hide();
			
			var originalName = $('#name').val();
			var originalContents = $('#contents').val();
			var originalInstructor = $('#instructor').val();
			
			var regName = /^20\d{2}(0[1-9]|1[1-2])$/
			var regContents = /^\w+(,\s\w+)*$/

			$('#name').blur(function() {
				$('#nameError').text('');
					if ($('#name').val().trim() === '') {
						$('#nameError').text('クラス名前を入力してください').css('color','red');
						$('#name').removeClass('has-success');
						$('#name').addClass('has-error');
						return;
					}
					if (!regName.test($('#name').val().trim())) {
						$('#nameError').text('6桁の数字を入力してください（例：20YYMM）').css('color', 'red');
						$('#name').removeClass('has-success');
						$('#name').addClass('has-error');
						} else {
							$('#name').removeClass('has-error');
							$('#name').addClass('has-success');
							$('#nameError').text('✓').css('color', 'green');
						}
					})

			$('#contents').blur(function() {
				$('#contentsError').text('');
					if ($('#contents').val().trim() === '') {
						$('#contentsError').text('コンテンツを入力してください').css('color', 'red');
						$('#contents').removeClass('has-success');
						$('#contents').addClass('has-error');
						return;
							}
					if (!regContents.test($('#contents').val().trim())) {
						$('#contentsError').text('正確な格式で入力してください（例：Java, Python）').css('color', 'red');
						$('#contents').removeClass('has-success');
						$('#contents').addClass('has-error');
						} else {
							$('#contents').removeClass('has-error');
							$('#contents').addClass('has-success');
							$('#contentsError').text('✓').css('color','green');
						}
					})

			$('#instructor').blur(function() {
				$('#instructorError').text('');
					if ($('#instructor').val().trim() === '') {
						$('#instructorError').text('講師の名前を入力してください').css('color', 'red');
						$('#instructor').removeClass('has-success');
						$('#instructor').addClass('has-error');
						return;
						}
					if ($('#instructor').val().length > 0) {
						$('#instructor').removeClass('has-error');
						$('#instructor').addClass('has-success');
						$('#instructorError').text('✓').css('color','green');
						}
					})
			
			$('#updateForm').on('submit', function(e) {
				e.preventDefault();
				
				if ($('#nameError').text().length > 1
					|| $('#contentsError').text().length > 1
					|| $('#instructorError').text().length > 1) {
					
					$('#overlay').fadeIn({duration:270, start:function() {$(this).css({'display':'grid', 'place-items':'center'})}});
					$('#overlay').html(`
						<div id="dialog" class="dialog" tabindex="-1">
							<h4 class="dlgTitle">更新不可</h4>	
							<div class="center">
								<h5>入力項目を確認してください</h5>
							<br>
							</div>
							<div class="center">
								<input id="return" type="button" value="戻る" class="btn btn-warning">
							</div>
						</div>
							`)
					$('#return').focus();
					
					$('#return').on('click', function() {
						$('#overlay').hide();
						$('#overlay').off('keydown.focusTrap');
						return;
					})
					
					$('#overlay').on('keydown.focusTrap', function(e) {
						if(e.key === 'Tab') {
							const active = document.activeElement;
							
							if(!e.shiftKey && active === $('#return')[0]) {
								e.preventDefault();
								$('#return').focus();
							} else if(e.shiftKey && active === $('#return')[0]) {
								e.preventDefault();
								$('#return').focus();
							}						
						} else if(e.key === 'Escape') {
							$('#overlay').hide();
							$('#overlay').off('keydown.focusTrap');
						}
					})		
				} else {
					const id = $('#kurasuId').val();
					const name = $('#name').val().trim();
					const contents = $('#contents').val().trim();
					const instructor = $('#instructor').val().trim();
					const memo = $('#memo').val().trim() ? $('#memo').val().trim() : '';
					const memoShow = memo ? '備考：' + memo : '';
					
					if(name === originalName) {
						$('#name').removeClass('has-error');
						$('#name').addClass('has-success');
						$('#nameError').text('✓').css('color', 'green');
					}
						
					if(contents === originalContents) {
						$('#contents').removeClass('has-error');
						$('#contents').addClass('has-success');
						$('#contentsError').text('✓').css('color', 'green');
					}
						
					if(instructor === originalInstructor) {
						$('#instructor').removeClass('has-error');
						$('#instructor').addClass('has-success');
						$('#instructorError').text('✓').css('color', 'green');
					}
					
					$('#overlay').fadeIn({duration:270, start:function() {$(this).css({'display':'grid', 'place-items':'center'})}});
					$('#overlay').html(`
						<div id="dialog" class="dialog" tabindex="-1">
							<h4 class="dlgTitleMsg">入力項目確認</h4>	
							<div class="center">
								<p>クラス名前：${name}</p>
								<p>コンテンツ：${contents}</p>
								<p>担当先生：${instructor}</p>
								<p>${memoShow}</p>
							</div>
							<br>
							<div class="center">
								<input id="confirm" type="button" value="更新" class="btn btn-info update">
								<input id="return" type="button" value="戻る" class="btn btn-warning">
							</div>
						</div>
					`)
					
					$('#confirm').focus();
					
					$('#overlay').on('keydown.focusTrap', function(e) {					
						if(e.key === 'Tab') {
							const active = document.activeElement;
							
							if(!e.shiftKey && active === $('#return')[0]) {
								e.preventDefault();
								$('#confirm').focus();
							} else if(e.shiftKey && active === $('#confirm')[0]) {
								e.preventDefault();
								$('#return').focus();
							}						
						} else if(e.key === 'Escape') {
							$('#overlay').hide();
							$('#overlay').off('keydown.focusTrap');
						}
					})		
					
					$('#confirm').click(function() {
						$.ajax({
							url:'/updateKurasuInfo',
							type:'post',
							data:{KurasuId:id, KurasuNamae:name, KurasuKamoku:contents, tantoSensei:instructor, memo:memo},
							success:function() {
								$('#panel-body1').html(`
									<div style="text-align:center">
										<p>更新しました</p>
										<input type='button' value='戻る' onclick="location.href='/kurasu'" class="btn btn-warning">
									</div>	
										`)
							},
							error:function() {
								alert('更新できませんでした')
							}
						})			
						$('#overlay').hide();
						$('#overlay').off('keydown.focusTrap');
					})
					
					$('#return').click(function() {
						$('#overlay').hide();
						$('#overlay').off('keydown.focusTrap');
					})
				}
			})
			
			$('#insertForm').on('submit', function(e) {
				e.preventDefault();
				
				if ($('#name').val() == "" 
						|| $('#contents').val() == "" 
					|| $('#instructor').val() == ""
					|| $('#nameError').text().length > 1
					|| $('#contentsError').text().length > 1
					|| $('#instructorError').text().length > 1) {
					
					if($('#name').val() == "") {
						$('#nameError').text('クラス名前を入力してください').css('color','red');
						$('#name').removeClass('has-success');
						$('#name').addClass('has-error');
					}
					
					if($('#contents').val() == "" ) {
						$('#contentsError').text('コンテンツを入力してください').css('color', 'red');
						$('#contents').removeClass('has-success');
						$('#contents').addClass('has-error');
					}
					
					if($('#instructor').val() == "") {
						$('#instructorError').text('講師の名前を入力してください').css('color', 'red');
						$('#instructor').removeClass('has-success');
						$('#instructor').addClass('has-error');						
					}
					
					$('#overlay').fadeIn({duration:270, start:function() {$(this).css({'display':'grid', 'place-items':'center'})}});
					$('#overlay').html(`
						<div id="dialog" class="dialog" tabindex="-1">
							<h4 class="dlgTitle">登録不可</h4>	
							<div class="center">
								<h5>入力項目を確認してください</h5>
							<br>
							</div>
							<div class="center">
								<input id="return" type="button" value="戻る" class="btn btn-warning">
							</div>
						</div>
							`)
					$('#return').focus();
						
					$('#return').on('click', function() {
						$('#overlay').hide();
						$('#overlay').off('keydown.focusTrap');
						return;
					})
						
					$('#overlay').on('keydown.focusTrap', function(e) {
						if(e.key === 'Tab') {
							const active = document.activeElement;
								
							if(!e.shiftKey && active === $('#return')[0]) {
								e.preventDefault();
								$('#return').focus();
							} else if(e.shiftKey && active === $('#return')[0]) {
								e.preventDefault();
								$('#return').focus();
							}						
						} else if(e.key === 'Escape') {
							$('#overlay').hide();
							$('#overlay').off('keydown.focusTrap');
						}
					})	
				} else {
					const name = $('#name').val().trim();
					const contents = $('#contents').val().trim();
					const instructor = $('#instructor').val().trim();
					const memo = $('#memo').val().trim() ? $('#memo').val().trim() : '';
					const memoShow = memo ? '備考：' + memo : '';
					
					$('#overlay').fadeIn({duration:270, start:function() {$(this).css({'display':'grid', 'place-items':'center'})}});
					$('#overlay').html(`
						<div id="dialog" class="dialog" tabindex="-1">
							<h4 class="dlgTitleMsg">入力項目確認</h4>	
							<div class="center">
								<p>クラス名前：${name}</p>
								<p>コンテンツ：${contents}</p>
								<p>担当先生：${instructor}</p>
								<p>${memoShow}</p>
							</div>
							<br>
							<div class="center">
								<input id="confirm" type="button" value="登録" class="btn btn-info update">
								<input id="return" type="button" value="戻る" class="btn btn-warning">
							</div>
						</div>
					`)
					
					$('#confirm').focus();
					
					$('#overlay').on('keydown.focusTrap', function(e) {					
						if(e.key === 'Tab') {
							const active = document.activeElement;
							
							if(!e.shiftKey && active === $('#return')[0]) {
								e.preventDefault();
								$('#confirm').focus();
							} else if(e.shiftKey && active === $('#confirm')[0]) {
								e.preventDefault();
								$('#return').focus();
							}						
						} else if(e.key === 'Escape') {
							$('#overlay').hide();
							$('#overlay').off('keydown.focusTrap');
						}
					})		
					
					$('#confirm').click(function() {
						$.ajax({
							url:'/insertKurasuInfo',
							type:'post',
							data:{KurasuNamae:name, KurasuKamoku:contents, tantoSensei:instructor, memo:memo},
							success:function() {
								$('#panel-body2').html(`
									<div style="text-align:center">
										<p>登録しました</p>
										<input type='button' value='戻る' onclick="location.href='/kurasu'" class="btn btn-warning">
									</div>	
										`)
							},
							error:function() {
								alert('登録できませんでした')
							}
						})			
						$('#overlay').hide();
						$('#overlay').off('keydown.focusTrap');
					})
					
					$('#return').click(function() {
						$('#overlay').hide();
						$('#overlay').off('keydown.focusTrap');
					})
				}
			})
		})
	</script>
</body>
</html>